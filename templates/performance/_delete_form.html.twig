<form method="post" action="{{ path('performance_delete', {'id': performance.id}) }}" onsubmit="return confirm('Etes-vous sûr(e) ?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ performance.id) }}">
    <button class="btn btn-danger my-3">Supprimer</button>
</form>
