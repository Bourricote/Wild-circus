{% extends 'base.html.twig' %}

{% block title %}Numéros{% endblock %}

{% block body %}
    <div class="container">
    <h1>Tous les numéros</h1>
        <a href="{{ path('performance_new') }}" class="btn btn-primary my-3">Ajouter</a>

        <table class="table admin-table">
        <thead>
            <tr>
                <th>Nom</th>
                <th>Catégorie</th>
                <th>Créé le</th>
                <th>Image</th>
                <th class="text-center">actions</th>
            </tr>
        </thead>
        <tbody>
        {% for performance in performances %}
            <tr>
                <td>{{ performance.name }}</td>
                <td>{{ performance.category.name }}</td>
                <td>{{ performance.createdAt ? performance.createdAt|date('d/m/Y') : '' }}</td>
                <td><img src="/upload/{{ performance.imageName }}" alt="vignette" class="vignette"></td>
                <td class="text-center">
                    <a href="{{ path('performance_show', {'id': performance.id}) }}"><i class="fas fa-search-plus mr-2"></i></a>
                    <a href="{{ path('performance_edit', {'id': performance.id}) }}"><i class="fas fa-pencil-alt"></i></a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="7">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    </div>
{% endblock %}
