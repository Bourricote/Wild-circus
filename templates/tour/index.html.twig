{% extends 'base.html.twig' %}

{% block title %}Dates{% endblock %}

{% block body %}
    <div class="container">
    <h1>Dates de tournée</h1>
        <a href="{{ path('tour_new') }}" class="btn btn-primary my-3">Ajouter</a>

    <table class="table admin-table">
        <thead>
            <tr>
                <th>Ville</th>
                <th>Date</th>
                <th>Prix</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tour in tours %}
            <tr>
                <td>{{ tour.city }}</td>
                <td>{{ tour.date ? tour.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ tour.price }}€ </td>
                <td>
                    <a href="{{ path('tour_show', {'id': tour.id}) }}">voir</a>
                    <a href="{{ path('tour_edit', {'id': tour.id}) }}">modifier</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
    </div>
{% endblock %}
