{% extends 'base.html.twig' %}

{% block title %}Tour index{% endblock %}

{% block body %}
    <h1>Tour index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>City</th>
                <th>Date</th>
                <th>Price</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for tour in tours %}
            <tr>
                <td>{{ tour.id }}</td>
                <td>{{ tour.city }}</td>
                <td>{{ tour.date ? tour.date|date('Y-m-d H:i:s') : '' }}</td>
                <td>{{ tour.price }}</td>
                <td>
                    <a href="{{ path('tour_show', {'id': tour.id}) }}">show</a>
                    <a href="{{ path('tour_edit', {'id': tour.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('tour_new') }}">Create new</a>
{% endblock %}
