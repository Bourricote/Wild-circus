{% extends 'base.html.twig' %}

{% block body %}
    <div class="container">
        <div class="row mb-5">
            <div class="col-md-12">
                <h1>Réserver des places</h1>
            </div>
        </div>
        <div class="row mb-5">
            <div class="col-12">
                <h3>{{ tour.city }} {{ tour.date ? tour.date|date('d/m/Y à G:i') : '' }}</h3>
            </div>
            <div class="col-12">
                {{ form_start(form) }}
                {{ form_widget(form) }}
                <button class="btn btn-primary my-3">Réserver</button>
                {{ form_end(form) }}
            </div>
        </div>
    </div>

{% endblock %}
