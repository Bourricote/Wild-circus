{% extends 'base.html.twig' %}

{% block title %}Fan{% endblock %}

{% block body %}
    <div class="container">
        <h1>Fan</h1>

        <table class="table admin-table">
            <tbody>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Roles</th>
                <td>{{ user.roles ? user.roles|json_encode : '' }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ user.firstname }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.lastname }}</td>
            </tr>
            </tbody>
        </table>

        <a href="{{ path('user_index') }}" class="btn btn-primary my-3">Retour</a>

        <a href="{{ path('user_edit', {'id': user.id}) }}" class="btn btn-primary my-3">Modifier</a>

        {{ include('user/_delete_form.html.twig') }}
    </div>
{% endblock %}
