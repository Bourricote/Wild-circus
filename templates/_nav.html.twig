<nav class="navbar navbar-expand-lg">

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent"
            aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"><i class="fas fa-dragon"></i></span>
    </button>
    <a class="navbar-brand" href="{{ path('home') }}">Avatar Circus</a>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav mr-auto">
            <li>
                <a class="nav-link" href="{{ path('all_performances') }}">Numéros</a>
            </li>
            <li>
                <a class="nav-link" href="{{ path('all_tours') }}">Tournées</a>
            </li>
            <li>
                <a class="nav-link" href="{{ path('game') }}">Jeu</a>
            </li>
            <li>
                <img class="nav-img" src="{{asset('build/images/seed-left.png')}}">
            </li>
        </ul>

        <ul class="navbar-nav ml-auto">
            <li>
                <img class="nav-img" src="{{asset('build/images/seed-right.png')}}">
            </li>
            {% if is_granted('ROLE_ADMIN') %}
                <li>
                    <a class="nav-link" href="{{ path('admin') }}"> Admin</a>
                </li>
            {% endif %}
            {% if app.user %}
                <li class="nav-link">
                    Mon compte
                </li>
                <li>
                    <a class="nav-link" href="{{ path('app_logout') }}"> Se déconnecter</a>
                </li>
            {% else %}
                <li>
                    <a class="nav-link" href="{{ path('app_register') }}"> S'inscrire</a>
                </li>

                <li>
                    <a class="nav-link" href="{{ path('app_login') }}"> Se connecter</a>
                </li>
            {% endif %}

        </ul>
    </div>
</nav>
